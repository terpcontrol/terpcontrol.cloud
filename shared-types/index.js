// Runtime values shared by the server and the webapp. Types live in index.d.ts
// and are erased at compile time; anything here is real code both sides import.
module.exports = {
  OKAM_STREAM_PREFIX: 'okam://',
};

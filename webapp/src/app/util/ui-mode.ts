/**
 * localStorage key for the settings Simple/Expert toggle. 'true' = expert.
 * Shared so the setup wizard can drop the user back into simple mode after
 * a guided setup.
 */
export const EXPERT_MODE_STORAGE_KEY = 'app-settings-expert';
